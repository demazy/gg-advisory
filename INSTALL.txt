GG Advisory Grants Radar 7.0-snapshot-sentinel

Install the package into the gg-advisory repository without replacing config/grants.yaml.
The package contains the verification contracts, fresh evidence snapshots, dated discovery baseline, zero-API runtime, tests, PDF builder and GitHub workflow.

The installer validates that the current visible canonical registry still matches the supplied evidence snapshots before it reports PASS.
